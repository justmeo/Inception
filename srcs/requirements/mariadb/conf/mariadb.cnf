[server]
# Safer defaults; we are on an internal bridge network only.
max_connections=200
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci

[client]
default-character-set = utf8mb4

[mysqld]
skip-name-resolve